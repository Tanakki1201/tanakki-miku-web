// MIKU WORLD v2 — zero-cost-first frontend configuration
// Leave API_BASE empty to stay in clearly-labeled DEMO mode.
// Only the public Worker URL belongs here. No API key is ever placed in the browser.

window.MIKU_WORLD_CONFIG = {
  API_BASE: "https://miku-world-observer.tanakki-miku-world.workers.dev"
};